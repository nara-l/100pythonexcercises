items = []
for item in range(1,21):
    items.append(item)


# using list comprehension
#items = [item for item in range(1,21)]

print (items)

# 3rd way
items = range(1,21) # range creates python range object.

#print (list(items)) # list on range object converts it to a list
